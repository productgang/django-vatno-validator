============
Installation
============

At the command line::

    $ easy_install django-vatno-validator

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-vatno-validator
    $ pip install django-vatno-validator
